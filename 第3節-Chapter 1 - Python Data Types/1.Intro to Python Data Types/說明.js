// ❗ Data Types
// 第一章很重要要背起來
// 1. 規則與語法
// 2. 有嚴格的大小寫
// 3. 使用 空格 形成 blocks ，其他語言都是使用 ; 或是 {} 來形成 blocks
// 4. 鍵盤上的 Tab 等於 4 個 空白建 ， 所以必須要都統一 ，一開始就使用 tab 就全部都是 tab
// 5. 8 個基本類型                    Copy By Reference
//      int(integer)                    No
//      float(float)                    No
//      str(string)                     No
//      bool(boolean)                   No
//      list(list)                      Yes
//      dict(dictionaries)              Yes
//      tup(tuples)                     Yes
//      set(sets)                       Yes